{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run main",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "Build with Make"
        }
    ]
}